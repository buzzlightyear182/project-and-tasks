<% content_for :title, "Create a new AWESOME project" %>
<hr class='divider'>

<div class='dropdown'>
  <%= form_for @project do |f| %>
    <h2><%= f.label "Project name:" %></h2>
    <%= f.text_field :name %>
    <%= f.submit "Save", :class => 'save' %>
  <% end %>
</div>
